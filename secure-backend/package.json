{
  "name": "secure-chat-backend",
  "version": "1.0.0",
  "description": "Secure backend server that keeps API keys private and never exposes them to frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node -e \"console.log('Backend tests coming soon')\"",
    "check-env": "node -e \"require('dotenv').config(); console.log('API Keys loaded:', { gemini: !!process.env.GEMINI_API_KEY, openai: !!process.env.OPENAI_API_KEY })\""
  },
  "keywords": [
    "secure",
    "backend",
    "api",
    "chat",
    "gemini",
    "openai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "@google/generative-ai": "^0.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
